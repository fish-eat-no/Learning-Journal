# 学习日志 - 2026.04.12 🚀

## 📚 学习内容
1. 内核级线程切换的代码实现
3、FreeRTOS中的删除任务

---

## 📝 LeetCode 刷题记录

| 题号 | 题目 | 思路 |
|:----:|:----:|:-----|
|      |      |      |

---

## 💡 重点/难点记录

### 重点
1. 内核级线程切换的代码实现
3、FreeRTOS中的删除任务
   


### 难点
1. 内核级线程切换的代码实现

---

## ⚠️ 遇到的问题
-1、8(%ebp) 指向第一个参数（next_pcb）为什么*8以后就是下一个PCB的地址？
-2、调用者保存寄存器是不是就是我们user能使用的寄存器
-3、ebp其实是一个基址指针，他和esp指针配合使用，则可以记录一个栈帧，这么理解对吗？
-4、.align 2 的功能和作用
-5、必须说明一点就是按下复位键以后，蜂鸣器的状态是一直再叫，也就是说，任务是间断叫，但是我现在需要的是按下复位键，蜂鸣器是不叫的，直达我按下按键Pb1他开始任务，间断的叫，然后按下PB11，他删除任务，不叫
---

## ✅ 解决方案
-1、8(%ebp) 指向第一个参数（next_pcb 指针），然后对这个指针解引用并加上偏移 8（即 *(next_pcb + 8)），访问的是 PCB 结构体中位于偏移 8 处的成员——通常就是指向下一个 PCB 的指针。
-2、适合短生命周期的临时变量，减少压栈/弹栈开销（因为子函数可以直接覆盖它们）。
-3、ebp 与 esp 配合，完整定义并记录了一个栈帧的边界和内部布局（其实就是ss和sp）
-4、在汇编语言中，.align 2 是一个对齐伪指令，用于指定后续代码或数据在内存中的起始地址必须满足一定的对齐要求
-5、现象：本例程实现的是
  烧写复位后，OLED没显示，LED常量，蜂鸣器处于初始化状态->一直处于振动状态，持续的叫
  按下按键PB1后，OLED会显示“Music start”，LED常量，创建任务，然后蜂鸣器状态处于工作状态->每间隔1s叫一次
  按下按键PB11后，OLED会显示“MUSic Stop”，LED灭，删除任务，然后蜂鸣器状态处于待机状态->不震动,不叫
  状态机：
  [上电/复位]
       ↓
  STATE_INIT (初始化)
       ↓ (自动跳转)
  STATE_IDLE (待机)
   ↗        ↖
PB1│        │PB11
   ↓        ↙
STATE_MUSIC_RUN ←→ STATE_MUSIC_STOP

本例程出现的一个困扰我很久bug
在我进行复位之后，蜂鸣器随之振动发出叫声，此时按下PB1，我在PA0的LED灯会熄灭，
然后，显示屏第四行出现“Music start”的字符串，
然后再按下PB11后，显示屏显示Music stop LED灯亮，但是蜂鸣器一直在响，
这不就说明，我们的任务删除并没有实现吗？
后面解决问题：
删除 Task1/2/3 和互斥量后能控制蜂鸣器，是因为消除了 OLED 共享资源的竞争，
按键任务不再被阻塞或延迟，从而能及时响应按键并删除蜂鸣器任务。 
这是 FreeRTOS 开发中“最小化共享资源，避免不必要的同步”原则的典型体现。

---

## 🔗 参考链接

- [操作系统原理（bilibili）](https://www.bilibili.com/video/BV19r4y1b7Aw/?spm_id_from=333.337.search-card.all.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
- [FreeRTOS（bilibili）](https://www.bilibili.com/video/BV1Jw411i7Fz/?spm_id_from=333.1387.favlist.content.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  

