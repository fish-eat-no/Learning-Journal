# 学习日志 - 2026.04.12 🚀

## 📚 学习内容
1. 内核级线程切换的代码实现  
2. FreeRTOS 中的删除任务  

---

## 📝 LeetCode 刷题记录

| 题号 | 题目 | 思路 |
|:----:|:----:|:-----|
|      |      |      |

---

## 💡 重点/难点记录

### 重点
1. 内核级线程切换的代码实现  
2. FreeRTOS 中的删除任务  

### 难点
1. 内核级线程切换的代码实现  

---

## ⚠️ 遇到的问题

1. `8(%ebp)` 指向第一个参数（`next_pcb`），为什么 `*8` 以后就是下一个 PCB 的地址？  
2. 调用者保存寄存器是不是就是用户能使用的寄存器？  
3. `ebp` 是一个基址指针，它与 `esp` 配合使用可以记录一个栈帧，这样理解对吗？  
4. `.align 2` 的功能和作用是什么？  
5. 复位后蜂鸣器一直叫，按下 PB1 开始间断叫，按下 PB11 删除任务却无效；最终发现是资源共享导致按键响应延迟，删除多余任务和互斥量后解决。  

---

## ✅ 解决方案

1. **`8(%ebp)` 指向下一个 PCB 的地址**  
   `8(%ebp)` 指向第一个参数（`next_pcb` 指针），对该指针解引用并加上偏移 8（即 `*(next_pcb + 8)`），访问的是 PCB 结构体中位于偏移 8 处的成员——通常就是指向下一个 PCB 的指针。  

2. **调用者保存寄存器的用途**  
   适合存放短生命周期的临时变量，减少压栈/弹栈开销（因为子函数可以直接覆盖它们）。  

3. **`ebp` 与 `esp` 的作用**  
   `ebp` 与 `esp` 配合，完整定义并记录了一个栈帧的边界和内部布局（本质上就是 `ss` 和 `sp` 的协同）。  

4. **`.align 2` 的功能**  
   在汇编语言中，`.align 2` 是一条对齐伪指令，用于指定后续代码或数据在内存中的起始地址必须满足 4 字节对齐（2² = 4）。  

5. **蜂鸣器任务删除失败的问题**  
   - **原始现象**：  
     - 复位后：OLED 无显示，LED 常亮，蜂鸣器持续振动（一直叫）。  
     - 按下 PB1：OLED 显示 “Music start”，LED 亮，蜂鸣器每 1 秒叫一次。  
     - 按下 PB11：OLED 显示 “Music Stop”，LED 灭，但蜂鸣器仍然一直叫 → 任务删除未生效。  
   - **状态机**：
   - [上电/复位] → STATE_INIT → STATE_IDLE
↗ ↖
PB1 PB11
↙ ↗
STATE_MUSIC_RUN ↔ STATE_MUSIC_STOP
   - - **根本原因**：多个任务与互斥量共享 OLED 资源，导致按键任务被阻塞或延迟，无法及时响应 PB11 去删除蜂鸣器任务。  
- **解决方法**：删除多余的任务（Task1/2/3）和互斥量，消除对 OLED 共享资源的竞争，使按键任务能立即响应并成功删除蜂鸣器任务。  
- **经验总结**：FreeRTOS 开发中应遵循 **“最小化共享资源，避免不必要的同步”** 原则。  

---

## 🔗 参考链接

- [操作系统原理（bilibili）](https://www.bilibili.com/video/BV19r4y1b7Aw/?spm_id_from=333.337.search-card.all.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
- [FreeRTOS（bilibili）](https://www.bilibili.com/video/BV1Jw411i7Fz/?spm_id_from=333.1387.favlist.content.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
