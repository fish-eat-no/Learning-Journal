
# 学习日志 - 2026.05.21 🚀

## 📚 学习内容
1. 进程树
2. FreeRTOS 中优先级和阻塞
3. FreeRTOS 任务栈与堆空间的关系
4. 多任务运行时资源分配问题

---

## 📝 LeetCode 刷题记录

| 题号 | 题目 | 思路 |
|:----:|:----:|:-----|
| - | - | 今日未刷题，主要精力放在操作系统进程树、FreeRTOS 任务优先级、阻塞状态以及任务栈/堆空间问题的理解与调试上 |

---

## 💡 重点/难点记录

### 重点
1. 进程树  
   进程树用于描述进程之间的父子关系，可以帮助理解操作系统中进程的创建、管理和资源继承关系。

2. FreeRTOS 中优先级和阻塞  
   FreeRTOS 通过任务优先级决定任务调度顺序。高优先级任务在就绪状态下会优先运行；当任务进入阻塞状态后，CPU 会让给其他就绪任务。

3. 任务栈与系统堆空间  
   每个任务创建时都需要分配独立栈空间，任务数量和栈大小都会消耗 FreeRTOS 的堆空间。

### 难点
1. FreeRTOS 中优先级和阻塞  
   难点在于理解任务并不是一直运行的，而是在“就绪、运行、阻塞”等状态之间切换。

2. 栈大小和堆大小的关系  
   一开始容易误以为只要单个任务栈不大就没问题，但实际上所有动态创建任务的栈空间都会从 FreeRTOS 堆中分配。

---

## ⚠️ 遇到的问题

1. 依旧还是之前的问题，一旦任务栈大小设置太小，就会导致功能异常。  
2. 创建多个任务时，如果每个任务栈分配过大，系统堆空间不足，任务可能创建失败，进而导致程序现象异常。  
3. 表面上看像是 OLED、蜂鸣器或按键功能异常，本质上可能是任务没有正常创建或调度。

---

## ✅ 解决方案

1. 查看 FreeRTOS 堆空间配置：

```c
#define configTOTAL_HEAP_SIZE ((size_t)3072)
```

发现当前 FreeRTOS 堆大小为 3072 字节。

2. 理解任务栈大小的单位：  
   在 STM32F103 这种 32 位平台上，`xTaskCreate()` 中的栈大小单位通常是 word，不是 byte。

例如：

```c
xTaskCreate(Task1, "Task1", 256, NULL, 3, NULL);
```

这里的 `256` 大约对应：

```text
256 * 4 = 1024 字节
```

3. 因此如果创建 3 个栈大小为 256 的任务：

```text
256 * 3 = 768 word
768 * 4 = 3072 字节
```

刚好接近或达到 `configTOTAL_HEAP_SIZE`，再加上 FreeRTOS 内部控制块、队列、信号量等内存开销，就容易导致任务创建失败。

4. 正确理解：  
   任务栈大小不能只看单个任务，而要计算所有任务栈、任务控制块、队列、信号量等总内存占用。

5. 后续优化方向：
   - 减小不必要的任务栈大小。
   - 增大 `configTOTAL_HEAP_SIZE`。
   - 使用 `uxTaskGetStackHighWaterMark()` 观察任务实际剩余栈空间。
   - 检查 `xTaskCreate()` 的返回值，判断任务是否创建成功。

---

## 🔗 参考链接

- [操作系统原理（bilibili）](https://www.bilibili.com/video/BV19r4y1b7Aw/?spm_id_from=333.337.search-card.all.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)
- [FreeRTOS（bilibili）](https://www.bilibili.com/video/BV1Jw411i7Fz/?spm_id_from=333.1387.favlist.content.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)
```
