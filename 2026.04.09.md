# 学习日志 - 2026.04.09 🚀

## 📚 学习内容
1. 操作系统原理中的内核级线程
2. 完成了key的驱动的写完

---

## 📝 LeetCode 刷题记录

| 题号 | 题目 | 思路 |
|:----:|:----:|:-----|
|      |      |      |

---

## 💡 重点/难点记录

### 重点
1. 操作系统原理中的内核级线程
2. 完成了key的驱动的写完
   


### 难点
1. 操作系统原理中的内核级线程
2. 完成了key的驱动的写完

---

## ⚠️ 遇到的问题
xTaskCreate(Key_OLED, "Key_test", 128, NULL, osPriorityNormal, NULL);
这一条语句如果我是128的栈大小，那么我可以如愿的在OLED的第四行显示我们的light open的提示词，但是如果我将栈大小的定义为256，则第四行无任何输出，但是呢前三行在128/256的时候都是正常运行的，这是为什么？


---

## ✅ 解决方案
三、根本原因：缺少互斥量导致的竞态条件
1. 资源共享冲突
OLED 屏幕是共享资源，被四个任务同时访问：

三个显示任务（受互斥量保护）

Key_OLED 任务（无保护）

当 Key_OLED 任务调用 OLED_ShowString 或 OLED_Clear 时，它可能与正在执行显示任务发生并发写入。由于没有互斥量，可能出现：

显示任务正在更新第一行，Key_OLED 同时写入第四行 → 部分数据撕裂，但通常仍能显示。

更严重的是：OLED_Clear() 会清空整个屏幕，如果显示任务紧接着重写前三行，而 Key_OLED 在清屏后写入第四行，那么显示效果取决于谁最后写入。

2. 为什么栈=128 时能显示，栈=256 时不能？
关键在于时序窗口：

栈=128 时：

Key_OLED 任务可能因为栈较小，其就绪列表中的位置或调度优先级权重（相同优先级下，时间片轮转）使得它在按键触发后获得较长的连续执行时间。

在它完成 OLED_ShowString 写入第四行后，其他显示任务的下一次调度可能稍晚（例如正好错过 500ms 定时器的触发点），因此第四行的内容能保持可见，直到下次被覆盖。

栈=256 时：

更大的栈可能改变了任务的内存布局，导致调度器的时间片分配或任务就绪队列顺序发生微小变化。

Key_OLED 任务执行 OLED_ShowString 后，立即被一个显示任务抢占（因为相同优先级，时间片轮转）。该显示任务获取互斥量后，刷新前三行（但不会主动清除第四行）。

然而，OLED_ShowString 的底层驱动可能涉及写缓冲区或页寻址。如果显示任务在 Key_OLED 写入后马上也调用 OLED_ShowString（更新前三行），由于没有互斥量保护，两者的 I2C/SPI 通信可能交织，导致第四行的数据被破坏或未真正写入屏幕。

---

## 🔗 参考链接

- [操作系统原理（bilibili）](https://www.bilibili.com/video/BV19r4y1b7Aw/?spm_id_from=333.337.search-card.all.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
- [FreeRTOS（bilibili）](https://www.bilibili.com/video/BV1Jw411i7Fz/?spm_id_from=333.1387.favlist.content.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  

