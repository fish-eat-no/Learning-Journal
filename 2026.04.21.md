# 学习日志 - 2026.04.21 🚀

## 📚 学习内容
1. 内存分页
2. 拓宽我的磁盘

---

## 📝 LeetCode 刷题记录

| 题号 | 题目 | 思路 |
|:----:|:----:|:-----|
|      |      |      |

---

## 💡 重点/难点记录

### 重点
1. 内存分页
2. 拓宽我的磁盘

### 难点
1. 内存分页
2. 拓宽我的磁盘

---

## ⚠️ 遇到的问题
这几天算是低谷期，但是慢慢恢复吧，也好几天没记录了，就把这几天的学习日志写在这里。


---

## ✅ 解决方案

  
  2026/4/17
  1、今天的任务简单，就是把电位器跑通加上DMA传输,

  2026/4/18
  1接着昨天的任务，我接着昨天的任务继续完善功能，我们需要将这个电位器的值转化为数据，用它的大小控制
  我们蜂鸣器的大小，而且根据日常任务开发，我们将这个数值定在0-100之间，所以需要先处理数据
  
  2026/4/20
  先是手动修复了一个代码bug，做了以下几件事情，1、增加了堆的大小 2、调整了优先级 3、增加了互斥保护量
  1：  #define configTOTAL_HEAP_SIZE                    ((size_t)3072)->
    #define configTOTAL_HEAP_SIZE                    ((size_t)3072*2)
	2：  xTaskCreate(Key_OLED, "HighTaskHandle",128, NULL, 4, &HighTaskHandle);
  xTaskCreate(BuzzerMusic_Task, "MidTaskHandle",128, NULL, 3, &MidTaskHandle);
  xTaskCreate(OLED_Task, "LowTaskHandle",128, NULL, 2, &LowTaskHandle);
  xTaskCreate(ADCPot_Task,"ADCPot_Task",256,NULL,1,NULL);
  3、   if(xSemaphoreTake(sem, portMAX_DELAY) == pdTRUE) // 等待 ADC 信号量 
        {
            adc_value = ADC_Pot_Read(); // 读取 ADC 转换结果
            if (xSemaphoreTake(g_OLEDMux, pdMS_TO_TICKS(50)) == pdTRUE)
            {
                OLED_ShowString(4,1,"ADC:");
                OLED_ShowNum(4, 5, adc_value, 4); // 在 OLED 第0行第0列显示 ADC 值（4位）
                xSemaphoreGive(g_OLEDMux);
            }
        }
		
	关键应该就是第三点增加了这个互斥量，因为我们两个任务都同时向OLED发送他们自己的任务，就导致出现了同时向OLED
	写入数据，导致了乱码的现象，刚好就和这段时间在学的OLED资源共享这个知识点呼应上了，我们增加一个互斥锁就可以很好的
	解决问题，在解决代码的时候有学会了这一点吗，加深了理解。
	2026/4/21
  今天对这个例程进行复盘，首先是这个例程最主要学习的内容就是我们的互斥量的使用，使用互斥量，对临界区进行保护，从而实现
  资源合作的效果，也就是我们的按键和ADC任务都需要使用OLED这个共享资源，所以我们需要使用互斥量来保护这个资源，避免出现竞争条件，从而实现我们的功能。
  ，这就是本例程最重要的地方
  其次，我们还对DMA和ADC进行了配置，使用DMA进行数据传输，使用ADC进行数模转化，这也是本例程的一个重要内容。
  最后，我们还对任务的优先级进行了调整，合理使用了CPU资源
  我先建立一个简短计划追踪步骤，然后打开并读取关键源文件以提炼实现要点。

更新 TODO 状态：已完成文件读取，标记下一步为分析驱动与任务。

正在启动: *分析任务与外设驱动（蜂鸣器、ADC、按键、OLED）* (2/4)

总结（简明）：

- 功能概览：实现一个基于 FreeRTOS 的小型“音乐/蜂鸣器+显示+按键+电位器”演示，包含按键控制、OLED 显示、蜂鸣器鸣叫、ADC（电位器）+DMA 读取与任务间同步。

- 任务与优先级：
  - 高优先级：按键扫描与命令发送（检测 PB1/PB11，发送音乐命令）。
  - 中优先级：蜂鸣器/LED 控制任务，按队列命令启动/停止并按 g_BeepIntervalMs 周期切换输出。
  - 低优先级：OLED 显示任务，定期显示各任务心跳计数器与状态（受互斥量保护）。
  - 额外：ADC/DMA 处理任务，等待二值信号量由 DMA/ADC 中断或驱动给出。

- 互斥与同步：
  - 使用互斥量保护 OLED 显示缓冲区（避免并发显示冲突）。
  - 使用队列（g_MusicCmdQueue）传递音乐命令（MUSIC_CMD_START/STOP）。
  - 使用二值信号量通知 ADC/DMA 采样完成（ADCPot_Task 使用该信号量读取并处理 ADC 值）。

- 外设与驱动：
  - 蜂鸣器：GPIO 控制（开/关/翻转），由中优先级任务周期性翻转实现鸣响/闪烁；LED 与蜂鸣器同步翻转显示状态。
  - OLED：通过 GPIO 模拟 I2C 驱动（写命令/写数据、字符/数字显示、清屏等）。
  - ADC + DMA：ADC 采样通过 DMA 将结果放入缓存，ADCPot_Task 接收信号量后读取并将 ADC 值缩放为 0–100 的“音量”显示。
  - 按键：轮询消抖（PB1/PB11），短按发送不同命令并调整 g_BeepIntervalMs（示例中 PB1、PB11 设置不同间隔）。

- 行为/状态机（实现与注释中描述）：
  - 上电/复位 → STATE_INIT → STATE_IDLE。
  - 按 PB1 → 进入 STATE_MUSIC_RUN（OLED 显示 “Music start”，开始按周期鸣叫）。
  - 按 PB11 → 进入 STATE_MUSIC_STOP（OLED 显示 “Music stop”，停止鸣叫）。
  - 注：代码中以队列命令与任务控制实现这些状态转换。
---

## 🔗 参考链接

- [操作系统原理（bilibili）](https://www.bilibili.com/video/BV19r4y1b7Aw/?spm_id_from=333.337.search-card.all.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
- [FreeRTOS（bilibili）](https://www.bilibili.com/video/BV1Jw411i7Fz/?spm_id_from=333.1387.favlist.content.click&vd_source=7395aa6a2c0024d0e425d7f1cf153b4c)  
